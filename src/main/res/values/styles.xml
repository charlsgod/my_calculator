<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">?themePrimary</item>
        <item name="colorPrimaryDark">?themePrimaryDark</item>
        <item name="colorAccent">?themeAccent</item>
    </style>

    <style name="AppTheme.NoActionBar.First">
        <item name="themePrimary">@color/lightGray</item>
        <item name="themePrimaryDark">@color/darkGray</item>
        <item name="themeAccent">@color/orange</item>
        <item name="fontOperatorsColor">@color/white</item>
        <item name="fontNumbersColor">@color/white</item>
        <item name="fontDigitsColor">@color/white</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="AppTheme.NoActionBar.Second">
        <item name="themePrimary">@color/nacre</item>
        <item name="themePrimaryDark">@color/gray</item>
        <item name="themeAccent">@color/purple</item>
        <item name="fontOperatorsColor">@color/white</item>
        <item name="fontNumbersColor">@color/black</item>
        <item name="fontDigitsColor">@color/black</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:fontFamily">@assets/fonts/Avenir-Light</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- This will set the fade in animation on all your activities by default -->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>
